services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: cardvault
      POSTGRES_USER: cardvault
      POSTGRES_PASSWORD: cardvault
    ports:
      - "5432:5432"
    volumes:
      - cardvault_pgdata:/var/lib/postgresql/data

volumes:
  cardvault_pgdata:
